// Automatically generated by PRGA's blackbox library generator
module {{ module.vpr_model }} (
    {%- set comma = joiner(", ") %}
    {%- for port in module.ports.values() %}
    {{ comma() }}{{ port.direction.case('input', 'output') }} wire [{{ port|length - 1}}:0] {{ port.name }}
    {%- endfor %}
    );
    {%- for param, attributes in (module.parameters|default({})).items() %}
    parameter {{ param }} = {{ attributes.default }};
    {%- endfor %}
endmodule

